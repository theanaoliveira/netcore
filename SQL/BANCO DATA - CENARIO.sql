CREATE TABLE IF NOT EXISTS SCENARIO (
	ID_MODULE VARCHAR(20),
    ID_USER INT,
    ID_SCENARIO INT,
    SCEN_NAME TEXT,
    SEARCH VARCHAR(100),
    EMPRESA TEXT,
    GRUPO_ANALISE TEXT,
    PERIODICIDADE VARCHAR(50),
    FLAG_MARCA INT,
    SCEN_TYPE CHAR(1),
    LANGUAGE VARCHAR(50),
    LOCATION VARCHAR(1000),
    FLAG_LOCATION INT,
    TYPE_PERIOD CHAR(5),
    MAP_SELECTION CHAR(10)
);

CREATE TABLE IF NOT EXISTS SCENARIO_OCORRENCIA (
	ID_SCENARIO INT,
	WORD TEXT,
    QTD INT,
    ORIGEM VARCHAR(50),
    ITEMPESQUISA VARCHAR(1000),
    STATUS VARCHAR(100),
    IDPOST TEXT
);

CREATE TABLE IF NOT EXISTS SCENARIO_SENTIMENTO (
	ID_SCENARIO INT,
	ITEM VARCHAR(50),
    POSITIVO INT,
    NEUTRO INT,
    NEGATIVO INT,
    PERIOD TEXT,
    IDPOST TEXT
);

CREATE TABLE IF NOT EXISTS SCENARIO_LOCATION (
	ID_SCENARIO INT,
	ITEMPESQUISA TEXT,
    STATUS VARCHAR(100),
    LATITUDE VARCHAR(100),
    LONGITUDE VARCHAR(100),
    ORIGEM VARCHAR(50),
    ADDRESS TEXT,
    IDPOST TEXT
);

CREATE TABLE IF NOT EXISTS SCENARIO_STANDARD (
	ID_SCENARIO INT,
	ID_USER INT,
	ID_MODULE TEXT,
	SCEN_TYPE CHAR(1)
);